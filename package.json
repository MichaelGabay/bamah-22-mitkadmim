{
  "name": "auth-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run run:all",
    "start": "npm run i:all && npm run run:all",
    "run:all": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "i:all": "npm install && npm install --prefix server && npm install --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
